{
  "name": "video-watermark-remover",
  "version": "2.0.0",
  "description": "100% client-side (browser + WebAssembly) app that removes a watermark/logo from a video by selecting the region on a preview frame. No server, no install.",
  "private": true,
  "scripts": {
    "build": "node scripts/copy-vendor.js",
    "start": "npm run build && node scripts/dev-server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@ffmpeg/core": "0.12.10",
    "@ffmpeg/ffmpeg": "0.12.15",
    "@ffmpeg/util": "0.12.2"
  }
}
